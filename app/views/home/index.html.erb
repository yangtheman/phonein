<div class="row">
  <div class="hero-unit">
    <h1>Phone In</h1>
    <p>
      Check in and out by making a phone call and following SIMPLE directions.
    </p>
    <p>
      <% if admin_signed_in? %>
        Welcome back, <%= current_admin.email %>
      <% else %>
        <%= link_to "Sign-up Today!", new_admin_registration_path, :class => "btn btn-primary btn-large"%>
      <% end %>
    </p>
  </div>
    <% if admin_signed_in? %>
      <div class="span6">
        <%= link_to "Add client", clients_path, :class => "btn btn-primary btn-large" %>
      </div>
      <div class="span6">
        <%= link_to "Add agent", agents_path, :class => "btn btn-primary btn-large" %>
      </div>
    <% end %>
</div>
  